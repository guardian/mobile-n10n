stacks: [mobile-notifications]
regions: [eu-west-1]
deployments:
  backup-ami:
    type: ami-cloudformation-parameter
    app: backup
    parameters:
      amiTags:
        Recipe: xenial-mobile
        AmigoStage: PROD
  backup:
    type: autoscaling
    parameters:
      bucket: mobile-notifications-dist
    dependencies: [backup-ami]

