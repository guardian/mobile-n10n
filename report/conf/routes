GET         /healthcheck                         @report.controllers.Report.healthCheck
GET         /notifications/:nType                @report.controllers.Report.notifications(nType: String, from: Option[DateTime], until: Option[DateTime])
