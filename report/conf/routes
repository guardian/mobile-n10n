GET         /healthcheck                        @controllers.Report.healthCheck
GET         /notifications/:nType                @controllers.Report.notifications(nType: String, from: Option[DateTime], until: Option[DateTime])
