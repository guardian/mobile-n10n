GET         /healthcheck                        @controllers.Report.healthCheck
GET         /notifications                      @controllers.Report.notifications(from: Option[DateTime], until: Option[DateTime])
